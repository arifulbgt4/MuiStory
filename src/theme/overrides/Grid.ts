import { Theme, Components } from "@mui/material/styles";

const MuiGrid: Components<Theme>["MuiGrid"] = {
  styleOverrides: {
    root: ({ theme, ownerState }) => ({}),
  },
};

export default { MuiGrid };
